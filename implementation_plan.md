# 視覚的仕分け機能 (Visual Sort) 改善計画

## ゴール
Visual Sort ウィンドウにおいて、ユーザーの使い勝手を向上させるため、以下の2点を実装するのじゃ。
1. **更新ボタンの追加**: フォルダ内の再スキャンや再分析を手動で行えるようにする。
2. **動的カラムレイアウト**: ウィンドウの幅に合わせて、画像サムネイルの列数（横に並ぶ数）を自動調整する。

## ユーザーレビューが必要な点
- **更新ボタンの挙動**: 「更新」ボタンは、フォルダ内のファイルを再スキャンし、AI分析（ベクトル比較）を最初からやり直す処理とするのじゃ。時間がかかる場合があるが、これで良いかの？

## 変更内容

### lib/GazoToolsGUI.py

#### [MODIFY] VisualSortWindow クラス

1.  **`__init__` メソッド**:
    - `<Configure>` イベント（リサイズ）をバインドし、サイズ変更時にレイアウトを再計算するようにするのじゃ。
    - `self.col_count` 変数を追加し、現在の列数を管理するのじゃ。

2.  **`init_control_frame` メソッド**:
    - 「更新 (Refresh)」ボタンを追加するのじゃ。
    - ボタンを押すと `start_analysis` を呼び出して再スキャンを行うようにするのじゃ。

3.  **`refresh_grid` メソッド**:
    - 現在は `col_count = 5` と固定されている部分を、`self.col_count` を使用するように変更するのじゃ。
    - グリッド配置のロジックはそのまま流用できるのじゃ。

4.  **`on_resize` メソッド (新規追加)**:
    - ウィンドウの幅（`self.winfo_width()`）を取得するのじゃ。
    - カードの幅（約210px = 幅200 + マージン）で割り、最適な列数を計算するのじゃ。
    - 列数が変わった場合のみ `refresh_grid` を呼び出すことで、無駄な再描画を防ぐのじゃ。
    - ※頻繁なイベント発火を防ぐため、少し遅延（after）させて処理するデバウンス処理を入れるのじゃ。

## 検証計画

### 手動検証
1.  **更新ボタンのテスト**:
    - Visual Sort ウィンドウを開く。
    - 外部でフォルダに新しい画像を追加または削除する。
    - 「更新」ボタンを押す。
    - 新しい画像がリストに反映されるか、削除された画像が消えるか確認する。
2.  **リサイズ機能のテスト**:
    - Visual Sort ウィンドウの端をドラッグして幅を広げる。
    - 横に並ぶ画像の数が増えることを確認する（例: 5列 → 6列 → 7列）。
    - 幅を狭める。
    - 横に並ぶ画像の数が減り、縦に伸びることを確認する。
